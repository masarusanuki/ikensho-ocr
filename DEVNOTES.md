# 開発メモ

このファイルは開発の記録です。実装の判断理由と、うまくいかなかった方法を残しています。

- 使い方 … [README.md](README.md)
- **実装を触る人向けの手順書（構成・全コマンド・新様式の追加手順）… [docs/DEVELOPER.md](docs/DEVELOPER.md)**
- 導入手順 … [docs/INSTALL.md](docs/INSTALL.md)

---

## 1. 何を作っているか

介護保険の主治医意見書を読み取るアプリ。要件は次のとおり。

- PDF（2ページ）・画像を、1つでも複数でもアップロードできる
- スマホ・タブレットのカメラでも撮れる（1ページずつのファイルになる前提）
- OCR で読む。ただし読み取れない前提で、**確認画面と編集画面**を必ず用意する
- 保存は JSON と CSV
- 項目は `master/` の様式に合わせる
- **LLM は極力使わない**
- 他でも使える形にする。インストールを簡単に。できれば1ファイル
- ローカル版と Web 版の両方
- 文字・チェックが読めている確信度を色で判別できるようにする
- 医療辞書を入れる。診療科しか入らない欄には診療科辞書。病名は ICD も。選択と補完を効かせる
- システムの管理画面も作る
- 開発メモはこのリポジトリに。公開分だけ `/var/www/html/test-deploy/kaigo_nintei/` に置く

---

## 2. 設計の要点

### 2.1 OCR に頼らない

意見書の項目 107 のうち、チェックボックスが 186 個（74項目）を占める。
残る 52 個のテキスト欄だけが OCR を要する。つまり**大半は OCR なしで読める**。

チェックボックス判定は画像処理だけで済み、手書き・印刷・スキャンいずれでも安定する。
日本語手書き OCR は現状どのエンジンでも精度が出ないため、
「読める部分を確実に読み、読めない部分は人が直す」という切り分けにした。
これが LLM を使わずに実用精度を出すための核心。

### 2.2 白紙様式との差分でマークを取る

> この節は最初の実装の話。**いまの判定は 7.4 に書き直したもの**を使っている。


当初は「枠の内側のインク率」だけで判定していたが、サンプルを見ると
**レ点は枠からはみ出して書かれる**ことが多く、枠内のインク率が 0.08 程度にしかならない。
枠を丸で囲む記入もある。

→ 記入前の様式画像（白紙）を持っておき、位置合わせ後に差分をとる方式に変更した。
印刷された枠線・ラベル・説明文がすべて消え、書き込みだけが残る。

```
diff = binarize(scan) AND NOT dilate(binarize(blank))
mark = diff の密度（枠 + 周囲45%の範囲）
```

`dilate` は位置ずれの許容。1回（約2px）に留めないと細いレ点まで消える（2回では消えた）。

最終的な判定は「枠内インク率」と「差分マーク」の**大きい方**を採用する。
枠内に収まったチェックは前者が、はみ出したレ点や丸囲みは後者が拾う。

公式様式の実測では、チェック済み 0.11〜0.13 / 未チェック 0.00〜0.006 と明確に分離した。

### 2.3 位置合わせは ORB + RANSAC

様式テンプレートの参照画像に対して特徴点マッチングし、射影変換で正規化する。
射影変換にしたのはカメラ撮影の遠近歪みに対応するため。

これが**様式の自動判別**も兼ねる。各テンプレートの各ページと照合し、
インライア比とインライア数からスコアを出して最良を選ぶ。
実測でスコアは、一致する様式 0.74〜0.92 / 別様式 0.02〜0.09 と明確に分かれた。

1ページずつ撮影された場合も、各ページが何ページ目かを個別に判定できる。
様式は複数ページの多数決で統一し、1ページ目が現れるたびに新しい1件として区切る。

---

### 2.4 チェック欄の「後ろの言葉」を読む

様式を Word で作り直すと、チェック欄の並びは同じでも**後ろの文字が変わる**ことがある
（「いくらか困難」→「ときどき困難」など）。定義に書いてある言葉をそのまま出力すると、
実物と食い違ったデータになる。

そこで読み取りのときに枠の右隣も OCR して確かめている（`labels.py` / `labels.js`）。

#### 2.4.1 行ごとにまとめて読む

1枠ずつ切り抜いて読むと 186 回 OCR を回すことになり、白紙様式で **87秒** かかった。
しかも2〜3文字だけでは読み違えが多い。

**同じ行に並ぶ枠をまとめて1回で読み**、読めた語の横位置から
「どの枠の言葉か」を振り分ける方式にした。31秒まで縮み、文字の前後が
つながるぶん読みも安定した。

読む前に、四角いチェック欄そのものを**白で塗りつぶす**。
塗らないと枠が「口」と読まれ、「口内科」のように語の頭に付いてしまう。

さらに読み取り時は、**印の付いた枠だけ**を読む（`only=`）。
出力に出るのは印の付いた枠の言葉だけなので、186枠を読む必要はない。
1通あたりの上乗せは18秒。様式全体は管理画面の「白紙様式から読み直す」で読める。

#### 2.4.2 読めた言葉はそのまま採らない

白紙様式（＝いちばん条件のよい画像）でも**完全一致は66%**だった。
ラベルの文字は高さ20pxほどしかなく、「同意しない」が「同意ない」、
「Ⅲa」が「皿a」になる。**読めた言葉をそのまま採ると出力がかえって壊れる。**

なので読み取りは「定義と食い違っているかもしれない」という**合図**として使い、
値に使う言葉は次の順で決める。

1. 管理画面で直した言葉
2. 定義（スキーマ）の言葉

「食い違っている」と言うための条件は、誤爆を抑えるためにかなり厳しくした。

| 条件 | 理由 |
|---|---|
| 読めた言葉が3文字以上 | 「有」「無」「Ⅳ」は読み違えが多すぎて判断できない |
| 定義の言葉を含まない（逆も） | 隣の語や手書きまで一緒に読めることがある |
| 定義との似かたが 0.5 以下 | difflib と同じ尺度。Python/JS で一致を確認済み |
| 同じ項目の**他の選択肢**にも似ていない | 振り分けを誤っただけの場合を除く |

この4条件で、白紙様式173枠のうち誤って「違う」と出たのは **1件**。
言い回しが3文字以上変わっている場合（「いくらか困難」→「ときどき困難」）は
きちんと検出できる。

#### 2.4.3 直せるようにする

管理画面に「チェック欄の言葉」の表を足した。項目 / 定義の言葉 / 読めた言葉 /
使う言葉（入力欄）の4列で、使う言葉を直すと確認画面と出力の両方に効く。
保存先はブラウザ内（`ikensho.labels.v1`）で、**利用者トークンで分けない**
（人の情報ではなく様式の設定なので）。
Python 版は `templates/labels/<様式ID>.json`（`/api/labels` で読み書き）。

---

## 3. テンプレートの作り方

### 3.1 公式様式（`official_v1`）

`master/主医師意見書.pdf` には**テキストレイヤがある**（Word 由来）。
`pdfplumber` で `□` 文字の座標をそのまま取れる。

- ページ1に 75 個、ページ2に 111 個、合計 186 個
- 取得した座標は em ボックスなので、レンダリング画像上の実際の輪郭にスナップする
- **186個すべてスナップ成功**（100%）

`□` の直後の文字列がその選択肢のラベルになるので、
`tools/form_definition.py` の `BOX_ORDER`（文書順に並べた選択肢の列）と照合して検証できる。
ラベル不一致は 0 件。

### 3.2 テキストレイヤの無い様式（`sample_v1`）

記入済みのスキャンしか無い場合。試した順に:

1. **単純な四角形検出** → 漢字の「日」「関」「医」「回」を大量に誤検出（131個 / 期待75個）
2. **複数サンプルの投票** → 誤検出も毎回同じ位置に出るので減らない
3. **「未記入なら枠内が空」という性質で投票** → 73個まで改善。ただし常にチェックされる枠を取り逃す
4. **位置合わせ後の画素を高パーセンタイル合成 → 白紙様式を復元** → **75/75, 111/111 で完全一致**

4 が正解だった。記入マークは一部の用紙にしか無いので、パーセンタイル 85〜93 の合成で消える。
中央値（50パーセンタイル）では、半数以上の用紙でチェックされる枠のマークが残ってしまう。

復元した白紙は、そのまま 2.2 の差分用の白紙としても使える。

検証は公式様式との**行構成の照合**で行う。各行のチェックボックス数の並びが一致するかを見る。
一度これで「利き腕の□左が未検出」を特定できた。

### 3.3 テキスト欄

公式様式は手で座標を定義した（52箇所）。罫線の位置は `pdfplumber` の
`page.rects` から取れるので、テキストエリアの上下端はそこから決めた。

別様式へは、チェックボックス186個を対応点にして写している。試した順に:

1. **薄板スプライン変換**（`cv2.createThinPlateSplineShapeTransformer` は
   opencv-python-headless 5.0 に無いので自前実装）→ 対応点が疎なヘッダ部で破綻し、
   矩形が紙面の外まで飛ぶ
2. **白紙をOCRしてラベル位置を対応点に追加**（マスター側はPDFのテキストレイヤから
   正確に取れる）→ p1 で8個、p2 で3個の対応点が増えたが、薄板スプラインのままでは
   かえって不安定になった
3. **単調な分離型写像**（x と y を独立に単調増加の折れ線で写す）→ 破綻しなくなった。
   ただし行ごとに列位置が違うため x が合わない
4. **行ごとの局所写像**（y は全体で単調な折れ線、x は近い行の対応点だけで直線近似）
   → 現在の実装。本文部はほぼ合う

**それでもヘッダ部は合わない。** マスターのヘッダには対応点が2個（同意する／しない）しか無く、
かつ2つの様式でヘッダの構造自体が違う（申請者ブロックの列構成が異なる）。
これは幾何変換では原理的に解けない。

**現在の扱い**: 転写した座標には `transferred: true` を付け、
そこから読んだテキストは確信度を半分にし、確認画面に
「欄の位置が暫定です（管理画面のテンプレート編集で調整できます）」と表示する。
潰れた矩形は出力しない。誤った位置から読んだ値を高い確信度で出すより、
「要確認」として人に渡すほうがよいと判断した。

**根本的な解決は管理画面のテンプレート編集**（座標をドラッグで直して書き出す）。
チェックボックス186個は正確なので、直すのはテキスト欄だけで済む。

### 3.4 残っている課題

- `sample_v1` のヘッダ部のテキスト欄座標。管理画面で手動調整して確定させるのが現実的
- 丸囲み方式（男・女、明・大・昭）の判定は、欄を等分してインク量を比べる素朴な方法。
  `sample_v1` では欄自体がずれているため誤りやすい

---

## 4. OCR まわり

### 4.1 エンジン

差し替え可能にした（`ocr.py` の `OcrEngine`）。

| エンジン | 導入 | 日本語 | 備考 |
|---|---|---|---|
| tesseract | システムに導入が必要 | jpn データが必要 | 精度は最も安定。推奨 |
| RapidOCR | pip のみ | 中国語モデル | 導入は楽。漢字は読めるがかな・字体に難 |
| なし | - | - | チェックボックスだけ読み、テキストは手入力 |
| tesseract.js | 不要（ブラウザ） | jpn 同梱 | ブラウザ版で使用 |

この開発機には tesseract を入れられなかった（sudo にパスワードが必要）ため、
RapidOCR で検証した。

### 4.1.1 エンジンの実測比較（同じ切り抜き画像で比較）

| 欄 | 正解 | tesseract | RapidOCR | manga-ocr |
|---|---|---|---|---|
| 医療機関名 | 牛久愛和総合病院 | 生ん愛和総合病院 | 生久爱和合病院 | ここで、年の経験経営者には |
| 診断名2 | 変形性膝関節症 | **変形性膝関節症** | 变形性膝節症 | ．．．変材推察課題に行きました。 |
| 住所 | 茨城県土浦市中央8-21-17 | 茨城県二浦市中央8-21-17 | 茨城土浦市中央8-21-17 | **茨城県土浦市中央８２１１７** |
| 特記（長文） | 独居であり、近隣に住む…… | **ほぼ全文正解** | 助詞が大量に脱落 | － |
| 申請者氏名（手書き） | 石井 とめ | （空） | 石井 | でも、石井とめでは、 |

分かったこと:
- **tesseract が最も正確**。特に複数行の文章はほぼ全文読める
- RapidOCR は中国語モデルなので字体が中国語になる。短い欄と数字には強い
- **manga-ocr は使えない**。生成型モデルのため、文字が少ないと文章を作り出す。
  ただし手書きの「石井とめ」を唯一読めており、候補としては価値がある。
  既定では使わず、明示指定時のみ「候補の1つ」として扱う
- 得意分野が違うので、**複数エンジンを併用して項目ごとに良い方を採る**方式にした（`--engine ensemble`）。
  時間は1〜2割増で済む

### 4.1.2 やってみて駄目だった前処理

tesseract の出力が空になる欄があったので、前処理を強めたところ**全体的に悪化した**。

| 前処理 | 結果 |
|---|---|
| 現状（照明平坦化＋弱いdenoise＋拡大） | 最良 |
| Otsu二値化 | 悪化（薄い文字が消える） |
| 罫線除去 | 大幅に悪化 |
| インク外接矩形に切り詰め | 大幅に悪化 |
| 2倍拡大、コントラスト強調 | 効果なし〜悪化 |

tesseract の LSTM はグレースケール画像を自前で処理する前提なので、
こちらで二値化すると情報を落としてしまう。**前処理は控えめが正解**。

一方、**出力の組み立てを直したら大きく改善した**。
- TSV の block/par/line 番号で行を復元する（単語ごとに改行していたため、
  複数行の欄が1文字1行になっていた）
- 日本語の文字間に入る空白を詰める（「石 井 と め」→「石井とめ」）。
  これをしないと辞書照合にかからない
- `tessedit_char_whitelist` は LSTM では悪影響が出る（郵便番号が `0 77` になった）。
  文字種の制限は後段のフィルタで行う
- 欄の形に応じて PSM 7/6（1行）と 6/4（複数行）を両方試し、確信度の高い方を採る

### 4.2 精度を上げるための後処理（LLM なし）

1. **簡体字・異体字の正規化** — RapidOCR は中国語モデルなので `变形性膝節症`
   `两下肢` `调` のように出る。字体変換表で日本語常用字体に寄せる（`kanji_norm.py`）。
   これだけで辞書の一致率が目に見えて上がった
2. **様式の印刷文言の除去** — テキスト欄の枠が説明文に食い込むと、OCR が様式自体の
   文言を読んでしまう。マスターPDFから定型文83行を抽出し、行ごとに照合して捨てる（`boilerplate.json`）。
   「記入）」のような短い切れ端も定型文の一部なら捨てる
3. **文字種の限定** — 日付・年齢・電話・郵便番号・ふりがなは書かれる文字が決まっている。
   tesseract には whitelist として渡し、他のエンジンでは後処理で落とす。
   効果例: `39.9 门` → `39.9`、`口 5 年 2 月 12 日` → `5 年 2 月 12 日`
4. **医療辞書との照合** — 一致度 0.72 以上なら辞書の正式表記を採用。
   それ未満でも候補として確認画面に出す

   当初は 2-gram の Dice 係数だけで照合していたが、これでは1文字違いを取り逃す
   （`骨粗葵症` と `骨粗鬆症` が候補にすら挙がらなかった）。
   **編集距離による部分一致**を加えて解決した。辞書語と同じ長さの窓を
   OCR結果の上で滑らせ、最も近い位置の編集距離で評価する。
   `（保存期）` のような余分な文字が付いていても拾える。

   | OCR結果 | 改善前 | 改善後 |
   |---|---|---|
   | 慢性裳臓病 (保存期 ) | 0.364（不採用） | **0.800（採用）** |
   | 骨粗葵症 | 候補なし | **0.750（採用）** |
   | 变形性膝節症 | 0.727 | 0.857 |
   | 心房紙動 | － | **0.750（採用）** |
   | 回提 6四6（無意味） | 候補なし | 候補なし（正しく棄却） |

   これで診断名3件がすべて辞書だけで正しく直るようになった。LLM は不要だった。

### 4.1.3 OCR を正解データで測れるようにした

「精度が悪い」を直すには、まず**測れる状態**にする必要がある。
それまでは数欄を目で見比べるだけで、モデルを変えて良くなったのか
分からなかった（DEVNOTES 4.1.1 の比較は5欄しかない）。

サンプルPDFにテキスト層は無いので、正解は作るしかない。

1. `tools/make_ocr_truth_sheet.py` … テキスト欄を切り抜いて番号を振り、
   縦に並べた画像を作る
2. その画像を見て、番号ごとの正解を書き取る → `bench/ocr_truth.json`
3. `tools/benchmark_ocr.py` … 同じ切り抜きを各エンジンに通して突き合わせる

**2検体 72項目（うち記入あり 50項目）**から始めた。少ないが、
「変えたら上がったのか下がったのか」は判断できる。

測る指標は4つ。

| 指標 | 意味 |
|---|---|
| 文字正解率 | 1 − 編集距離 ÷ 正解の文字数。部分的に読めた分も評価できる |
| 完全一致 | 記入のある欄で、正解とぴたり同じだった割合 |
| 空欄の判定 | 記入が無い欄を空と判断できた割合 |
| 拾い読み | 記入が無いのに2文字以上読んでしまった欄の数 |

位置合わせと欄の切り出しは共通なので、ここで見えるのは
**文字を読む部分だけ**の違いになる。

比べるときは `--raw`（辞書補正の前）でも見る。辞書が誤りを隠してしまい、
モデルの差が見えなくなるため。

### 4.1.4 認識モデルを日本語専用に変える

正解データで測ったら、**tesseract がいちばん弱かった**（文字正解率 50.5%）。
4.1.1 の「tesseract が最も正確」は5欄を目で見た比較で、母数が足りていなかった。

`rapidocr-onnxruntime` は認識モデルを差し替えられる。既定は**中国語向け**なので、
日本語のモデルに替える（`tools/fetch_ocr_model.py` で取得、`models/ocr/` に置く）。

モデルだけの差を見るため、**辞書補正をかけない生の読み**で比べた
（辞書が誤りを隠すと、モデルの違いが見えなくなる）。

| 認識モデル | 文字正解率 | 完全一致 | 1欄あたり |
|---|---:|---:|---:|
| **PP-OCRv4 日本語専用（japan_v4）** | **78.0%** | 40.0% | 0.67秒 |
| PP-OCRv5 server（多言語・大） | 78.0% | 56.0% | 9.7秒 |
| PP-OCRv3 日本語専用 | 71.9% | 44.0% | 0.67秒 |
| rapidocr 既定（中国語向け） | 66.7% | 28.0% | 0.65秒 |
| PP-OCRv5 mobile（多言語） | 66.4% | 36.0% | 0.81秒 |
| tesseract（それまでの既定） | 50.5% | 18.0% | 0.16秒 |

`japan_v4` を既定にした。server 版は完全一致では上だが**1欄10秒**（1件40欄で
6分以上）になるため、既定にはしない（選ぶことはできる）。

**日本語専用モデルが多言語モデルより良い**のは、字の種類が絞られている
（4,399字）ぶん取り違えが少ないからだと思われる。

### 4.1.5 罫線を切り落としてから読む

欄には印刷された罫線・カッコ・単位（`cm`）が入っている。認識モデルはそれも
文字として読もうとする。白紙様式との差分を見れば印刷と書き込みが区別できるので、
**書き込みのある範囲まで矩形を詰めてから**認識に回す（`textbox.ink_crop`）。

| | 詰めない | 詰める |
|---|---:|---:|
| japan_v4 | 78.0% / 40.0% | **84.6% / 60.0%** |
| tesseract | 50.5% / 18.0% | 62.1% / 48.0% |
| PP-OCRv5 mobile | 66.4% / 36.0% | 42.3% / 12.0% |

japan_v4 で **文字正解率 84.6%・完全一致 60.0%**。tesseract も上がる。
PP-OCRv5 mobile だけ悪化したが、既定にしないモデルなので追っていない。

詰めるのは**認識に渡す画像だけ**。読めた量の検算（4.2.2）には元の矩形を使う。
差分が取れない場合・書き込みが見当たらない場合・詰めた結果が8画素未満に
なる場合は、元の矩形のまま読む。

### 4.1.6 ブラウザでも同じモデルを動かす

ブラウザ版は tesseract.js だったので、Python 版と精度が揃わない。
そこで **onnxruntime-web** を同梱し、`models/ocr/` の認識モデルを
そのままブラウザで動かすようにした（`web/js/ppocr.js`）。

- **文字の検出はしない。** 欄の位置はテンプレートで分かっているので、
  切り抜きをそのまま認識に回す。複数行の欄だけ、インクのある行で切り分ける
- 前処理（高さ48・`(値/255-0.5)/0.5`）と CTC の復号（`['blank'] + 辞書 + [' ']`）は
  Python 版（rapidocr）と同じ規則に揃えている
- モデルが置かれていなければ tesseract.js に戻る
- `ort.env.wasm.numThreads = 1`。SharedArrayBuffer が使えない配信でも動くようにする

つまずいた点: `ort.env.wasm.wasmPaths` に相対パス（`vendor/`）を渡すと
`Failed to resolve module specifier` になる。ORT は動的 `import()` で
読み込むため、**絶対URL**を渡す必要がある。

同梱するのは**いちばん良いモデル1つだけ**（`--all-ocr-models` で全部入る）。
配るものが何十MBも増えるのを避けるため。

もう一つつまずいた点: ブラウザ版は Python 版より**空欄で拾い読みが多かった**
（22欄中10件）。Python 版の rapidocr は先に文字検出が走り、何も無い欄では
結果を返さないためだった。ブラウザ版は検出を省いているので、
**白紙様式との差分で「書き込みが無い」欄を先に弾く**ようにした
（Python 版にも同じ判定を入れた。もともと拾い読みが残っていたため）。

ブラウザ版の実測（辞書補正まで通した、画面に出る値）:

| | 文字正解率 | 完全一致 | 空欄の判定 | 拾い読み |
|---|---:|---:|---:|---:|
| tesseract.js（それまで） | 86.2% | 36.0% | 100% | 0件 |
| **PP-OCRv4 日本語専用** | **92.1%** | **56.0%** | 100% | 0件 |

Python 版は 93.0% / 60.0% なので、**ブラウザ版もほぼ同じところまで来た**
（残る差は、Python 版が先に文字検出を走らせていることによる）。

利用者が感じるのは**完全一致**の差だと思われる。36% → 56% は、
「直さずに済む欄が2割増えた」ということ。

### 4.1.7 ブラウザ版に文字位置の検出を入れた（日付が取れなかった件）

「生年月日の日が取れない」という指摘から調べたところ、**ブラウザ版の日付欄が
根本的に弱い**ことが分かった。原因は2つあった。

**① 印刷されている「日」を切り落としていた**

4.1.5 で「書き込みの範囲に詰めてから読む」ようにしたが、日付欄の
「年・月・日」は**印刷**なので書き込みの範囲に入らない。いちばん右にある
「日」が詰めた矩形の外に出て、`8年11月20日` が `8 年 11 月 20` になっていた。

そのうえ、日付の解釈は「数字＋区切り文字」の形しか拾えなかったため、
**末尾の「日」が無いと日が捨てられていた**。

対処:
- 日付欄には切り詰めをしない（印刷の区切り文字を残す）
- 解釈を作り直した。「月」より後の数字は、**「日」が無くても日として拾う**。
  逆に `8日年11月20` のように余分な字が挟まっても拾えるようにした。
  ありえない値（年>99・月>12・日>31）は捨てる

**② 文字の位置を見つけていなかった**

ブラウザ版は認識モデルだけを積み、切り抜き全体を1行として流していた。
日付欄は数字が離れて並ぶため、罫線や元号の丸印まで数字として読んでいた。

| 検体 | 正解 | 検出なし | 検出あり |
|---|---|---|---|
| A_type 生年月日 | 12年3月4日 | `172年3月4日4` | **12年3月4日** |
| A_type 最終診察日 | 6年5月14日 | `16年5月14日` | **6年5月14日** |
| B_hand 生年月日 | 8年11月20日 | `28年11月20日` | **8年11月20日** |
| B_hand 最終診察日 | 5年10月31日 | `11年50年101月81年…` | **5年10月31日** |
| C_hand 生年月日 | 17年2月2日 | `27年23日` | **17年2月2日** |
| C_hand 最終診察日 | 6年2月7日 | `61年1日` | **6年2月7日** |

途中で試して駄目だった案（記録として）:

- 枠（年・月・日）ごとに読む … 1桁の数字は単独ではほぼ読めない（18項目中4〜9一致）
- 書き込みの範囲に詰めて枠ごとに読む … さらに悪化（1桁が潰れる）
- 白い余白を足す量を変える … 変わらず
- 差分から「書き込みだけの白黒画像」を作って読む … 変わらず
- 差分で数字のかたまりに切って読む … かたまりは取れるが、1桁の認識が当たらない

**認識モデルだけでは、離れて並ぶ数字は読めない。** Python 版（rapidocr）は
先に検出を通しており、そこが差になっていた。同じ検出モデル
（`ch_PP-OCRv4_det_infer.onnx` 4.5MB）をブラウザにも積み、
DBNet の後処理（しきい値0.3 → 輪郭 → 外接矩形 → 確からしさ0.5 → 少し広げる）を
OpenCV.js で書いた。設定値は rapidocr の `config.yaml` と同じにしてある。

副産物として、**読めた文字の位置**が取れるようになった。
Python 版の `DigitReader` と同じ「位置で年・月・日に振り分ける」ことが
ブラウザでもできるようになる（今は区切り文字による解釈で足りている）。

つまずいた点: 見つけた枠を「切り抜きの高さの3割」で行判定していたため、
記述欄（数行）で**行が混ざり、文章の先頭が落ちた**
（`令和3年11月に…` が `残存している。抗血小板薬…` から始まる）。
枠の高さ（中央値）を目安に行をまとめるよう直した。
これで文字正解率が 91.5% → **93.2%** になり、Python 版（93.0%）に並んだ。

全体の実測（正解72項目・辞書補正まで通した値）:

| | 文字正解率 | 完全一致 | 空欄の判定 | 1件あたり |
|---|---:|---:|---:|---:|
| ブラウザ版 tesseract.js | 86.2% | 36.0% | 100% | － |
| ブラウザ版 日本語モデル（検出なし） | 92.1% | 56.0% | 100% | － |
| **ブラウザ版 日本語モデル（検出あり）** | **93.2%** | **60.0%** | 100% | 約22秒 |
| Python版 | 93.0% | 60.0% | 95.5% | 約45秒 |

なお A_type の最終診察日は、**Python 版が年を 5 と誤り、ブラウザ版は 6（正解）**
だった。振り分けの規則は同じで、違いは**その桁をどう読んだか**。
同じモデルでも切り抜きの取り方（検出された枠の広さ）で結果が変わる。
1桁の数字は、周りに余分なものが入るほど当たらなくなる。

### 4.2.1 記入の先頭が切れる問題（欄の左端を広げる）

測ったテキスト欄の左端が記入の先頭に食い込んでいて、最初の1〜2文字が
落ちることがあった。60検体で測ったところ、記入のある欄のうち

| 欄 | 記入あり | 左が切れていた |
|---|---:|---:|
| (4) 症状名 | 9 | 9 |
| (6) 運動 内容 | 21 | 14 |
| 特記すべき事項 | 16 | 11 |
| (6) 血圧 内容 | 14 | 10 |
| (7) 感染症 内容 | 7 | 6 |

はみ出しは最大 2.5% ページ幅ぶん。判定は**白紙様式との差分**で行った
（印刷が消えるので、書き込みだけが残る）。

一律に左へ広げると、こんどは印刷ラベルを読んでしまう。実測すると
「→ 対処方針 （」の**「針」の右半分**が入り込み、`対処方針` の読みが
`( 服薬管理と…` から `針 (服村管理と…` に悪化した。

そこで `textbox.widen_left()` は白紙様式を見て、次の条件でだけ広げる。

1. 欄の左端の**すぐ内側**（行の高さ×0.35以内）に印刷がある場合は広げない
   ── もともと印刷の際まで測れているので、広げる余地が無い
2. そうでなければ左へ走査し、印刷にぶつかる手前で止める
3. 印刷との間に行の高さ×0.35（最低3画素）の余白を残す
4. 上限はページ幅の 3%

結果、広がるのは 27 欄（`部位` 系・`内容` 系・症状名など）だけになり、
`対処方針` `申請者氏名` `郵便番号` は対象外になった。
実際に `褥瘡 部位` の読みが `第程度：` → `骨部程度：` と改善した。

### 4.2.2 読めた文字列を書き込みと突き合わせる

OCR の結果だけを見ても、正しいのか足りないのかが分からない。
`text_check.py` で、白紙様式との差分（＝書き込みだけ）から

- 行ごとの「**インクのある列数 ÷ 行の高さ**」＝ 書かれている量
- 書き込みが欄の**左右の端に接していないか**

を出し、読めた文字数と突き合わせる。

**1文字あたりの密度は 0.24〜1.70 とばらつく**（定型サンプル73欄で実測。中央値 0.72）。
狭く見積もると正しく読めている欄にまで警告が出るので、**幅を持たせて**
「見込みの下限 `密度÷1.70`」「上限 `密度÷0.35`」の外に出たときだけ拾う。

| 判定 | 条件 | 確信度 |
|---|---|---|
| **書き込みが見当たらない** | 密度 0.25 未満（1文字ぶんも無い）なのに文字が読めた | ×0.35 |
| 読み切れていない | 下限3文字以上で、読めた文字が下限の80%未満 | ×0.8 |
| 余計なものを拾った | 読めた文字が上限＋2文字を超える | ×0.8 |
| 先頭が切れている可能性 | 書き込みが欄の左端に接している | ×0.9 |
| 末尾が切れている可能性 | 書き込みが欄の右端に接している | ×0.9 |

いちばん効くのは**1行目**で、記入が無い欄に OCR が入れた文字（`摂食に関する留意事項＝い`、
`褥瘡の部位＝一ーー一` など）を拾う。`has_ink` は印刷された罫線にも反応するため、
差分を見ないとこれらは「確信度 高」のまま残ってしまう。
文字数の判定は文字種の決まった欄（数字・電話番号など）では行わないが、
この1行目だけは文字種によらず判定する。

理由は確認画面に注記として出す。**値は書き換えない**（読めた文字が少ないからといって
補うと、無い記載を作ってしまう）。消す場合は確認画面の「削除して確定」を使う。

あわせて、先頭・末尾に残った罫線・カッコ由来の記号を落とす（`trim_edges`）。
`（骨折` → `骨折`、`→ 経過：投薬管理` → `経過：投薬管理`、`血圧140/90）` → `血圧140/90`。

括弧は**対応が取れていないものだけ**落とす。当初は先頭の `（` を無条件に落としていたが、
`（右）大腿骨頸部骨折` が `右）大腿骨頸部骨折` になり、辞書の `大腿骨頸部骨折` に
当たらなくなっていた（レビューで発覚）。
`-3kg` の符号と、単独の `-`（「確認したが該当なし」の意思表示）も残す。
全部消える場合は元の文字列を返す。

**辞書を引く前**に行う。`（骨折` のままでは辞書の `骨折` に当たらないため。

### 4.2.3 医療機関名を一覧と突き合わせる

医療機関名には「病院・医院・クリニック」といった語尾の辞書を当てていたが、
これが**施設名を消していた**。

```
OCR生読み: 1 筑波記念病院記
確定値:    病院            ← 施設名が消える
```

部分一致の評価は、辞書語が読み取り結果に**そのまま含まれている**と満点になる。
`病院` が一致度1.0で採用されていた。`右大腿骨骨折` → `骨折` も同じ理屈で起きる。

対処: 辞書語が読み取り結果に含まれていて、しかも大幅に短い場合（7割未満）は置き換えない
（`_is_shortening`）。候補としては残す。あわせて、そのものが辞書にある場合は
一致度の順に関わらずそれを採るようにした（`脳梗塞後遺症` が `脳梗塞` に潰れていた）。

そのうえで、**厚生労働省の医療機関一覧**（`dict/hospitals/<都道府県>.json`）と突き合わせる
（`hospitals.py`）。所在地から都道府県を絞ってから照合する。

- 一致度 0.85 以上なら正式名称を採用し、所在地・電話が読めていなければ一覧の値で補う
- 決めきれない場合は候補として並べるだけ
- LLM に選ばせる場合も**一覧にある名前からしか選ばせない**

正式名称には「医療法人社団 常仁会 牛久愛和総合病院」のように開設者名が付くので、
前置きを外した施設名とも突き合わせる。ただし切れ端は使わない
（`アグリホームクリニック つくば` の `つくば` が `つくばセントラル病院` に
一致度1.0で当たってしまう。**同じ落とし穴を二度踏んだ**）。

読み取り側の前後のゴミも落としてから照合する（`土浦協同病院時` → `土浦協同病院`）。

| 読み取り | 結果 |
|---|---|
| `1 筑波記念病院記` | **筑波記念病院**（確信度0.90・採用） |
| `4 筐波記念病院` | 候補 筑波記念病院(0.833) |
| `土浦協同病院時` | 候補 総合病院土浦協同病院(0.769) |
| `病院` | 候補なし（短すぎる語では照合しない） |

### 4.2.5 医療機関一覧を47都道府県ぶん集める

厚生労働省の地方厚生局が「コード内容別医療機関一覧表」を公開しているが、
**局ごとに公開の仕方がまるで違う**。当初は関東信越の10都県しか取れていなかった。

| 局 | 形 | 名前 | 都道府県の見分け |
|---|---|---|---|
| 北海道 | xlsx 直リンク | `000495825.xlsx` | 書いていない（局が1県） |
| 東北 | xlsx 直リンク | `shitei-touhoku-ika-r0808.xlsx` | **1ファイルに6県のシート** |
| 関東信越 | zip | `shitei_ika_r0809.zip` | ファイル名の先頭コード |
| 東海北陸 | zip | `2609-01-01.zip` | 表題 |
| 近畿 | zip | `2026.9_kikanzentai_ika.zip` | ファイル名がローマ字（`osaka`） |
| 中国四国 | zip | `000496913.zip` | ファイル名に県名 |
| 四国 | zip | `000495939.zip` | 表題（リンク文字は「（ZIP）」だけ） |
| 九州 | zip | 事務所ごとに1つ、**97個並ぶ** | 表題 |

ファイル名では判断できないので、**中身を見て判定する**方式にした。
Excel の先頭に

```
コード内容別医療機関一覧表   [大阪府]
[令和 8年 9月 1日現在　医科　現存/休止]
```

と書かれているので、ここから 都道府県・医科／歯科／薬局・基準日 を読む。

決め方:

1. リンクの文字と**その手前の文章**で候補に点を付ける
   （表の中に「医科（PDF） 歯科（PDF） 薬局（PDF） エクセルデータ（ZIP）」と
   並ぶため、直前の文脈が効く）
2. 点の高い順に取りに行き、**中身を見て**医科の一覧表でなければ捨てる
3. 都道府県は ①表題の `[○○県]` ②シート名 ③ファイル名 ④局が1県だけ の順で決める
4. 同じ県で**基準日が新しいものだけ**を残す

つまずいた点:

- 北海道は**病院と診療所でファイルが分かれている**。1つ見つけたら止める作りでは
  519件（病院だけ）しか取れなかった。同じくらい確からしいファイルは続けて見る
- 九州は過去の月のファイルが同じページに並ぶ。全部拾うと**古い情報が混ざる**。
  基準日を見て、古いものを読んだ時点で打ち切る
- 東北は1ファイルに県ごとのシートが並ぶ。先頭シートだけ見ていては1県しか取れない

結果: **47都道府県 / 約9.5万件**。局のページ構成が変わっても自力で探せるよう、
設定したページで足りなければ局のサイトを辿って探す処理も入れてある。

管理画面の「最新版に更新」から取り直せる（サーバ版のみ）。
取得はバックグラウンドで動き、進捗（何県・何件）を画面に返す。
取れなかった都道府県は前回の内容を残す。**更新に失敗して一覧が消えるのが
いちばん困る**ため、書き出しは県ごとに行い、索引には前回ぶんも載せる。

### 4.2.4 記述の欄に1文字だけ

記載の無い欄で罫線やゴミを拾い、`い` `9` `1` のような1文字が入ることがある。
自由記述の欄（選択肢も文字種の指定も無い text / textarea）で1文字だけの場合は
**空にして要確認**にする。元の読みは `raw` に残るので「OCR生読み」から戻せる。

選択肢の欄（`有`・`無`・`右`・`左`）や数字の欄は対象外。
実測で、自由記述9欄が空になり、選択肢の1文字50欄はそのまま残った。

### 4.3 辞書

`dict/field_map.json` が項目ごとに使う辞書を決める。

- 診断名の欄 → 傷病名辞書（ICD-10 コードと介護保険の特定疾病フラグ付き）
- 他科受診の「その他」→ 診療科辞書
- 麻痺・拘縮・褥瘡の部位 → 部位辞書
- 感染症の欄 → 感染症辞書

傷病名は 181 件を収録（うち特定疾病 20 件）。
MEDIS 標準病名マスターのような大規模マスターは同梱していない（ライセンスと配布サイズのため）。
同じ形式に変換すれば管理画面から差し替えられる。

---

## 5. 確認・編集画面

要件の中心。

- 項目ごとに**確信度を色分け**（緑=高 / 黄=中 / 赤=低 / 紫=手動修正済み）
- 左に元画像、項目を選ぶと該当箇所を枠で示し、その部分を拡大表示する
- チェックボックスは選択肢ごとにインク率の実測値（%）を併記する。
  なぜそう判定したかが見えるので、直すべきか判断できる
- テキスト欄は辞書からの候補をボタンで出す。ICD-10 コードと特定疾病の別も表示
- 「次の要確認 ▶」で、確認が要る項目だけを順に送れる
- 「OCR生読み」を併記する。辞書補正がかかった場合に元の読みを確認できる
- 上部の集計（`読み取り 64 / 107 項目　確定 0　高 68　中 22　要確認 17　修正 0`）は
  **押すとその区分だけの表示になる**。同じものをもう一度押すと解除。
  「高だけを一覧して確定する」「修正した項目だけを見直す」といった使い方ができる。
  0件の区分は押せない。絞り込みで隠れている項目に飛ぶ操作（画像のクリックなど）を
  したときは、絞り込みを自動で解除する
- **元画像を破棄できる。** 元画像は保存していない（localStorage に入れるのは
  読み取った内容だけ）が、開いている間はメモリに載っている。
  席を離れるときや、続けて何件も読み取ってメモリが苦しいときのために、
  件ごと（「画像を消す（1.0 MB）」）と一括（保存・出力の画面）で捨てられるようにした。
  読み取った内容・確信度・注記は残る。操作ログにも残す

画面の分け方: 上部のメニューは **1. 読み取り / 2. 確認・編集 / 3. 保存・出力**（作業の順）と、
**管理 / 開発メモ**（設定と資料）を区切って並べる。
管理は様式テンプレート・辞書・しきい値・医療機関一覧・利用者・操作ログ、
開発メモはドキュメントと概要スライドへの入口。

確信度の出し方:
- チェックボックス: 判定境界（0.5）からの距離
- 単一選択: さらに 2位との差を見る。差が小さいほど下げる（どちらか判らない状態）
- テキスト: OCR の信頼度 × 辞書一致度。辞書に無い語は 0.7 倍に下げる
- 空欄と判定した欄は確信度 0.95（「空である」ことは確実に判る）

### 4.5 VLM（画像を見て答えるLLM）で読む方式

> 「VLM ベースのものも作ってください。ボタンで OCR の機能を切り替える感じ」

文字認識モデルとは**別の当たり方**をする手段として、画像を見て答える LLM で
欄を読む方式を足した（`vlm.py`）。読み取り画面のボタンで切り替える。

```
読み取り方式  [ 画像処理＋OCR ][ VLM ]
```

- モデルは GGUF（llama.cpp）。取得は `python3 tools/fetch_vlm_model.py`
  （既定 Qwen2.5-VL 3B / 約3.1GB）
- Python は `--engine vlm`。`ocr.get_engine()` の他のエンジンと同じ口に
  合わせたので、辞書照合・検算といった後段はそのまま通る
- ブラウザは1欄ぶんの切り抜きを `POST /api/vlm-read` に渡す。
  **画像はこの端末の Python プロセスに渡すだけで、外部には出ない**

#### 4.5.1 既定にはしない

PP-OCR のほうが速く、測った精度も上（4.1）。VLM を入れたのは

1. 手書きの崩れた字など、文字認識モデルが苦手な欄を別の方法で読めるようにする
2. どちらが良いかを**同じ正解データで比べられる**ようにする

の2点のため。`auto` では VLM を選ばない。

手書きの氏名では実際に差が出た。

| 欄 | PP-OCRv4 日本語専用 | VLM（Qwen2.5-VL 3B） |
|---|---|---|
| 申請者氏名（手書き） | `高橋 - × 7-` | `高橋トメ` |
| 医療機関名 | 筑波記念病院 | 筑波記念病院 |
| 郵便番号 | 305-0005 | 305-0000 |
| 住所 | 茨城県つくば市天久保2-14-3 | 茨城県つくば市天久保2-14-3 |

速さは**1欄あたり4〜18秒**（CPU）。PP-OCR の0.67秒に対して10〜25倍かかる。
1件で10分前後になるので、常用ではなく「この欄だけどうしても読めない」ときの手段。

#### 4.5.1.1 全欄を通すと現実的でない（実測）

ブラウザ版で1通まるごとを VLM で読ませたら、**40分かかっても終わらなかった**。
テキスト欄は52個あり、1欄5〜20秒なので当然の結果。

そこで確認画面に「**この欄をVLMで読み直す**」を足した。
まず画像処理＋OCRで全体を読み、**読めていない欄だけを指して読み直す**ほうが
現実的な使い方になる。押すと

1. ページ画像からその欄だけを切り出して `POST /api/vlm-read` に渡す
2. 返ってきた文字を値に入れ、注記に「VLMで読み直しました」を残す
3. **確信度は上げない**（`level = low` のまま）。必ず人が確かめる
4. 操作ログに残す（元の値も残るので戻せる）

読み取り方式の切り替え自体は残してある（全欄をVLMで読む比較のため）。
ボタンを押したときの説明に、1通10分以上かかることと、
上の使い方を案内する文を出している。

#### 4.5.2 歯止め

VLM は 4.4 の小型LLMと同じ性質を持つ。**読めない画像からもそれらしい文字を作る。**
そこで次の歯止めをかけている。

- 「読めない場合は 空 とだけ出力」と明示して聞く
- 説明・前置き・引用符を機械的に落とす。モデルは
  「画像には『脳梗塞後遺症』と書かれています」と返したがる。
  引用符でくくられていればその中だけを採り、
  「読み取れません」「文字はありません」系の返事は空として扱う
- 欄の文字種（`charset`）で後から絞る。既存の検算（文字数・端の文字）もそのまま通る
- **確信度を 0.55 で頭打ち**にする。要確認から漏れないので自動確定されない

#### 4.5.3 つまずいた点

**C 側のログが止まらない。** `Llama(verbose=False)` を渡しても
`image slice encoded in 11201 ms` のような行が1欄ごとに数十行出る。
画像を符号化する部分（mtmd/clip）はログの受け口を通らず、
**標準出力に直接書いている**。ログの受け口を空にするだけでは足りず、
呼び出しの間だけファイル記述子 1・2 を `/dev/null` に差し替えて捨てている。

**終了時に例外の山が出る。** インタプリタ終了に任せると llama.cpp の後始末が
**閉じた標準出力に書こうとして** `ValueError: I/O operation on closed file` を出す。
`atexit` で自分から閉じるようにした。

**日付欄の専用経路は使えない。** VLM は文字の位置を返さないので、
「年・月・日」で区切って数字を振り分ける読み方（4.2 の `DigitReader`）が成立しない。
VLM のときは欄全体を読み、文字列から解析する経路に落ちる。

---

### 4.4 小型LLMを試した結果

CPU で動く小型LLM（Qwen2.5-1.5B / llama.cpp）で OCR 結果を校正できるか試した。
1件あたり 0.5〜2秒、モデルは約1.1GB。

| OCR結果 | LLM出力 | 判定 |
|---|---|---|
| 骨粗葵症 | 骨粗鬆症 | ○ |
| 变形性膝節症 | 変形性膝関節症 | ○ |
| 整形夕科 | 整形外科 | ○ |
| 慢性裳臓病 (保存期 ) | 慢性膵炎 | × |
| **回提 6四6**（無意味な文字列） | **脳梗塞** | **危険** |

最後の1件が問題。**読み取り不能な文字列から、もっともらしい病名を自信ありげに作り出す。**
診療情報でこれを自動採用させるわけにはいかない。

そこで次の三重の制限をかけ、**候補提示のみ**に用途を絞った（`llm.py`）。
1. 出力は辞書に載っている語だけに限る（自由生成を許さない）
2. OCR結果と文字が一定以上重なっているものだけ採用する（`回提 6四6` → `脳梗塞` はこれで棄却）
3. 確信度は上げない。確認画面に「LLM候補」として並べるだけ

なお 4.2 の辞書照合を改善した結果、**上の表の失敗例2件は辞書だけで正しく直る**ようになった。
LLM は「辞書に無い表記ゆれ」への保険という位置づけ。既定では無効。

---

## 5.1 匿名化加工済みデータ（研究用）

研究用の意見書は氏名欄が白抜きされている。これを「読み取り失敗（確信度低）」として
赤く出すと、確認すべき項目が埋もれてしまう。

対応:
- 項目定義に `pii` を持たせた。`name`（氏名系）と `mask`（住所・連絡先系）の2種類
- 氏名欄がすべて空なら自動的に匿名化済みデータと判断し、値を `匿名化済み` にする
- 「匿名化加工済みデータ」を明示指定した場合は、住所・郵便番号・連絡先・
  医療機関名なども一律 `マスク済み` にする
- 匿名化した項目は確信度1.0・水色バッジとし、要確認の対象から外す
- **元の読み取り値と OCR 生読みは破棄する。** 表示だけ変えるのではなく値ごと捨てないと、
  JSON/CSV に個人情報が残ってしまう
- 解除しても元の値は戻せない（意図的にそうしている）。解除時は確認ダイアログを出す

出力には `anonymized` を入れて、匿名化済みかどうかを後から判別できるようにした。

---

## 5.3 操作ログと利用者トークン

読み取り結果を人がどこまで直したかを残す。読み取り精度の検証にも、
確認作業をした記録としても使う（`web/js/oplog.js`）。

残す操作:

| 記録 | いつ |
|---|---|
| `read_start` / `read_done` / `read_error` | 読み取りの開始・完了（件数と所要秒）・失敗 |
| `edit` | 項目の修正。直す前と直した後、読み取り時の確信度 |
| `confirm` / `unconfirm` / `confirm_group` / `confirm_visible` | 確定・解除・まとめて確定 |
| `clear_confirm` | 削除して確定 |
| `candidate` | 候補の採用（辞書／LLM／OCR生読みの別も残す） |
| `hospital` | 医療機関を一覧から選択 |
| `dictation` | 音声入力 |
| `kana_fix` | ふりがなの自動変換 |
| `anonymize` | 匿名化表示の適用・解除 |
| `export` / `import` | 書き出し・取り込み |

設計上の判断:

- **打ち込みの途中は記録しない。** 1文字ごとに残すと量が増えて読めなくなるので、
  0.9秒手が止まってから「打ち終わった値」を1件だけ残す。書き出しの前には
  `flush()` で打ちかけの分も確定させる
- **元に戻した場合は記録しない。** 直す前と直した後が同じなら捨てる
- **個人情報の項目（`pii` 付き）は値を残さず「○文字」だけにする。**
  それでも直した量が分かるように、`blen` / `alen` に文字数を別に持つ
- 値そのものを一切残したくない場合は、管理画面で「値も記録する」を外す
- 上限3000件。あふれたら古いものから捨てる。容量不足時は半分捨てて入れ直す

### 利用者トークン

読み取り結果も操作ログもブラウザの中だけに置くが、**同じ端末を複数の人が使うと
履歴が混ざる**。そこでブラウザごとにトークン（16進24文字）を発行し、
保存先の名前をトークンごとに分けた（`web/js/session.js`）。

```
ikensho.records.v1.<トークン>      読み取り結果
ikensho.oplog.v1.<トークン>        操作ログ
ikensho.settings.v1.<トークン>     しきい値
ikensho.token.v1                  今のトークン（これだけ共通）
```

- 初回に自動発行する。サーバには送らない
- 「新しく発行」すると、それまでの履歴は見えなくなる。**消してはいない**ので、
  元のトークンを入れ直せば戻せる（共用端末で使い終わったら発行し直す運用）
- 別の端末で続きをする場合は、トークンを入れ直す
- トークン導入前の保存先は、初回に今のトークンの下へ移す

なお、これは**同じブラウザの中での仕切り**であって、認証ではない。
localStorage を直接見られる相手には隠せない。サイト全体の保護は Basic 認証で行う。

---

## 5.2 出力するデータの形

保存は JSON と CSV の2種類。どちらも Python 版とブラウザ版で同じ形になる
（`python/ikensho_ocr/export.py` と `web/js/exporters.js` を対にして直すこと）。

### JSON

読み取った値だけでなく、**どれくらい確からしいか・何をどう直したか**を一緒に残す。
後から「この値は自動で読んだのか、人が直したのか」を追えるようにするため。

```json
{
  "exported_at": "2026-09-06T10:30:00+09:00",
  "schema_version": "1.0.0",
  "count": 2,
  "records": [
    {
      "schema_version": "1.0.0",
      "form_name": "主治医意見書",
      "template_id": "official_v1",
      "ocr_engine": "tesseract+rapidocr",
      "anonymized": false,
      "read_at": "2026-09-06T10:29:41+09:00",
      "sources": [
        { "source": "ikensho_0068.pdf", "source_page": 1,
          "page_index": 1, "matched": true, "score": 0.9189, "dewarped": false }
      ],
      "warnings": [],
      "values": {
        "applicant_name": "石井 とめ",
        "adl_disabled": "A2",
        "other_dept": ["歯科"],
        "paralysis": false,
        "height_cm": "148.6"
      },
      "meta": {
        "adl_disabled": {
          "label": "(1) 障害高齢者の日常生活自立度",
          "type": "choice", "section": "３．心身の状態に関する意見", "page": 1,
          "confidence": 0.86, "level": "high",
          "edited": false, "anonymized": false,
          "raw": null, "engine": null, "llm_candidate": null
        }
      }
    }
  ]
}
```

`values` の値は項目の型で決まる。

| 型 | 値 | 例 |
|---|---|---|
| `text` `textarea` | 文字列 | `"石井 とめ"` |
| `choice` `circle` | 選ばれた選択肢の文字列。未選択は `null` | `"A2"` |
| `multi` | 選ばれた選択肢の配列。未選択は `[]` | `["歯科", "内科"]` |
| `flag` | 真偽値 | `true` |

`meta` は項目ごとの付帯情報。

| キー | 意味 |
|---|---|
| `label` `type` `section` `page` | 項目定義から。CSVの列名だけでは分からないので同梱する |
| `confidence` | 確信度 0〜1 |
| `level` | `high` / `medium` / `low`。確信度を色分けの区分に落としたもの |
| `edited` | 人が確認画面で直したか |
| `anonymized` | 匿名化して置き換えた欄か |
| `raw` | OCR の生読み。辞書補正や誤字訂正がかかった場合に元が分かる |
| `date` | 日付欄のみ。`{year, month, day}` に分けたもの |
| `era` | 日付欄のみ。元号（明治／大正／昭和／平成／令和） |
| `gregorian` | 日付欄のみ。西暦に直したもの（`1956-01-25`）。元号と年が揃わないと `null` |
| `engine` | その欄を読んだ OCR エンジン（併用時にどちらを採ったか） |
| `llm_candidate` | LLM が出した候補。**採用値ではない** |

`sources` は元ファイルとの対応。1ページずつ撮影した場合でも
「どのファイルの何ページ目が、様式の何ページ目だったか」が残る。

| キー | 意味 |
|---|---|
| `source` `source_page` | 元ファイル名と、その中でのページ番号 |
| `page_index` | 様式の何ページ目と判定されたか |
| `matched` | 様式を判別できたか |
| `score` | 判別のスコア 0〜1。低いときは読み取りも怪しい |
| `dewarped` | 写真として台形補正をかけたか |

保存した JSON は読み戻せる（確認画面の「保存したJSONを読み込む」）。
続きから編集して再出力できる。

### 様式の形のJSON

上の JSON は項目IDを並べた平らな形で、機械で扱うには良いが**紙と見比べにくい**。
そこで、意見書の紙の並びそのままに入れ子にしたものも出せるようにした。

```bash
ikensho extract 意見書.pdf --form-json out.json
```
ブラウザ版は一覧画面の「様式の形のJSONで保存」。

```
節（基本情報／１．傷病に関する意見／…）
  └ まとまり（(3) 他科受診 など。様式で1つの枠に収まっているもの）
      └ 項目
```

**チェック欄は言葉で返す。** どの選択肢に印が付いたかが要なので、
値だけでなく選択肢の言葉を全部並べ、それぞれに印の有無を付ける。

```json
{
  "項目": "(3) 他科受診（診療科）",
  "種類": "複数選択",
  "値": ["内科", "整形外科"],
  "選択肢": ["内科", "精神科", "外科", "整形外科", "…"],
  "チェック": [
    { "言葉": "内科", "印": true },
    { "言葉": "精神科", "印": false },
    { "言葉": "整形外科", "印": true }
  ],
  "確信度": 1.0,
  "確信度の段階": "高",
  "項目ID": "other_dept"
}
```

言葉は**実物のもの**を使う（2.4 参照）。管理画面で直していればその言葉、
直していなければ定義の言葉。実物と定義が食い違って読めた場合は
`言葉の食い違い` に両方を残す。二重線で消された枠は `二重線で訂正: true`、
丸で囲まれた枠は `丸囲み: true` が付く。

キーを日本語にしたのは、**この JSON をそのまま人が読む**ことを想定しているため
（機械処理向けの平らな JSON は上のものを使う）。
Python 版とブラウザ版で同じ形になることは、実際に両方で出して
キーの集合と節・項目数を突き合わせて確認している。

### CSV

1行1件。**Excel で開けるよう BOM 付き UTF-8、改行は CRLF**。

先頭2行がヘッダになっている。

```
record_no,template_id,read_at,anonymized,sources,warnings,applicant_name,applicant_name__confidence,...
#,様式,読取日時,匿名化,元ファイル,警告,申請者氏名,確信度,...
1,official_v1,2026-09-06T10:29:41+09:00,いいえ,ikensho_0068.pdf,,石井 とめ,0.994,...
```

- 1行目 … 項目ID。プログラムで扱うときはこちらを使う
- 2行目 … 日本語の項目名。人が見るとき用
- 3行目以降 … 1件ずつ

各項目は**値と確信度の2列**が並ぶ（`<項目ID>` と `<項目ID>__confidence`）。
確信度を残すのは、あとから「確認が要る行」を絞り込めるようにするため。

複数選択の値は `；`（全角セミコロン）で区切る。カンマだと CSV の区切りと
紛らわしいため。`flag` 型は該当時に `該当`、非該当は空。

列数は項目定義の数で決まる（現在 107 項目 × 2 列 + 先頭6列 = 220 列）。
項目定義を増やすと列も増えるので、列名（1行目）で参照すること。位置で参照しない。

### 日付欄

記入日・生年月日・最終診察日・発症年月日は、OCR結果をそのまま文字列で持つのではなく
**年・月・日に分けて**持つ。確認画面で数字を埋めるだけで直せるようにするためと、
西暦に直して集計に使えるようにするため。

**元号は様式で決まっているので、日付欄では数字だけを読む。**
文字種を `date_digits`（数字と年月日頃のみ）にしてあり、元号の文字は候補に入れない。
元号を候補に入れると、そこが誤読の元になるため。
記入日の欄は「令和」が印刷済みなので、その分だけ読み取り範囲を右にずらしてある。

元号の取り方は項目ごとに違う。
- 記入日・最終診察日 … 様式に「令和」と印刷されているので固定（`default_era`）。
  本文中に元号らしき文字が読めても無視する
- 生年月日 … 別項目の「明・大・昭」の丸囲みから取る（`era_field`）
- 発症年月日 … 同じく「昭和・平成・令和」の丸囲みから取る

`value` には `31年1月25日` のように整形した文字列が入る。元号は含めない
（元号は別項目なので二重に持たない）。

### 派生値（機械学習で使いやすい形）

和暦のままでは比較も差分計算もできないので、**西暦に直したものを別に持つ**
（`python/ikensho_ocr/derive.py` と `web/js/exporters.js`）。
読み取った元の値はそのまま残し、派生値として並べる。

JSON では `derived`、CSV では**末尾にまとめた列**として出る。

| キー | 内容 |
|---|---|
| `<日付項目>_iso` | 西暦の ISO 形式。`1956-01-25`。日が読めていなければ `1956-01`、年だけなら `1956` |
| `<日付項目>_era` | 元号（明治／大正／昭和／平成／令和） |
| `<日付項目>_year` `_month` `_day` | 和暦の年・月・日（数値） |
| `age_computed` | 生年月日と記入日から計算した年齢 |
| `age_written` | 様式に書かれている年齢 |
| `age_matches` | 上の2つが一致するか（1歳差まで許容）。**読み取りの検算に使える** |

`age_matches` が `0` の行は、生年月日か年齢のどちらかを読み違えている可能性が高い。
確認すべき行を絞り込むのに使える。

### 匿名化したとき

`anonymized` が `true` になり、氏名は `匿名化済み`、住所・連絡先は `マスク済み` に
置き換わる。**このとき `raw`（OCRの生読み）も消す。** 表示だけ変えると
出力に個人情報が残ってしまうため。

---

## 6. 実装の分担

同じ判定ロジックを Python と JavaScript の両方に持たせている。重複だが、
「インストール不要のブラウザ版」と「大量処理できる Python 版」の両立には必要と判断した。
共有しているのは**データ**（項目定義・様式テンプレート・辞書）で、これが唯一の正典。

```
tools/form_definition.py     ← 項目マスタ（唯一の正典）
   ├→ schema/ikensho.schema.json   （両方が読む）
   └→ templates/official_v1.json   （両方が読む）
dict/*.json                  （両方が読む）
```

`tools/build_web.py` が `dist/web/` に、`tools/build_standalone.py` が単一HTMLに固める。

---

## 7. 検証

### 7.1 やったこと

- 公式様式のサンプル（`ikensho_00xx`）でチェックボックスの読み取りを目視確認 → 全項目正解
- 全 214 サンプルで一括処理 → 様式判別の失敗・警告 0 件
- Playwright でブラウザ版を実測。Python 版と同じ結果になることを確認
  （A2 / 自立 / 安定 / 同意する / 無 / 期待できない）
- 単一HTMLファイル版をオフライン（`file://`）で実測。2ファイルを投入して
  2件に自動分割され、それぞれ別様式として判別されることを確認

測定結果（必須単一選択18項目 × 214件 = 3,852 項目）:

| 様式 | 件数 | 検出率 | 確信度「高」の割合 |
|---|---|---|---|
| `official_v1` | 106 | 99.6% | 91.7% |
| `sample_v1` | 108 | 99.5% | 94.9% |

未検出は 12 項目のみ（`symptom_stability` 3、`other_psych_presence` 2、
`service_outlook` 2、`short_term_memory` 2、`infection` 2、`adl_dementia` 1）。

### 7.2 正解データが無い問題

サンプルに正解ラベルが無いため、精度を数値で出せていない。
代用として「必ず記入される単一選択項目が1つ選ばれているか」を見ている。

**本番投入前には、数十件でよいので正解データを作って測るべき。**
`tools/` に評価スクリプトを足す想定。

### 7.3 正解データ（seigo）で測る

100通ぶんの正解データ一式（`seigo/`。PDF 100件・チェック 18,600件・
文字 3,924件・印の種類13通り＋二重線で訂正47件）を頂いたので、
これに対して測る仕組みを作った。

```bash
python3 tools/benchmark_seigo.py --jobs 6 --marks   # チェック欄
python3 tools/benchmark_seigo.py --jobs 6 --text    # 文字欄
python3 tools/tune_checkbox.py dump --jobs 6        # 枠ごとの特徴量を書き出す
python3 tools/tune_checkbox.py eval                 # 規則を画像なしで試す
```

`tune_checkbox.py dump` は 18,600 枠の測定値を `bench/seigo_features.json` に
落とす（約60秒）。以後の規則の試行はこのファイルだけで回せるので、
1回1分ではなく1回1秒で試せる。**規則を触るときは必ずこれを使う。**

### 7.4 チェック欄の判定を作り直した（95.47% → 99.08%）

最初に測った結果がこれ。

| | 正解率 | 見落とし | 誤検出 | 二重線を消せた |
|---|---|---|---|---|
| 作り直す前 | 95.47% | 11 | **785** | 0/47 |
| 作り直した後 | **99.08%** | 46 | **117** | 39/47 |

誤検出785件は**すべて枠の中にインクが無い**ものだった。原因は差分マークを
「枠＋周囲45%」という広い窓で測っていたこと。**隣の欄の手書きを拾っていた**。

窓を広くしたのは「枠からはみ出したレ点」と「枠を丸で囲む記入」を拾うためだったが、
この2つは現れ方が違うので、1つの窓でまとめて測るのが間違いだった。書き方ごとに分けた。

| 見方 | 測る場所 | 拾う書き方 | 境界 |
|---|---|---|---|
| `fill` | 枠の内側（元画像） | 白紙が無いときの保険 | 0.19 |
| `ink` | 枠の内側（差分） | レ点・×・塗りつぶし・☑ など大半 | 0.04 |
| `ring` | 枠の外周（差分） | 枠を丸で囲む記入 | 0.20 |
| `out` | 枠の右下（差分） | 枠外にはみ出した印 | 0.03 |

`ring` には**左右の偏り**という条件を付けた。丸囲みは枠の周りがぐるりと濃くなるが、
隣の字を拾っただけなら片側に偏る。偏り0.5以下という条件だけで、
丸囲み183件のうち181件を保ったまま誤検出を44件→10件に減らせた。

`out` には**枠の右端とつながっているか**という条件を付けた。はみ出した印は
枠に掛かったまま外へ伸びるが、隣の手書きは枠に触れない。

白紙を引くときの膨張も3px→5pxにした。重ね合わせが数ピクセルずれると
**印刷された枠線そのものが差分に残り**、それを書き込みと見てしまうため。

### 7.5 二重線で消した印（47件）

「一度チェックしてから二重線で消した」箇所は、判定としては未チェックにしたい。
枠の中だけを見ると本物の印と区別がつかない（当然、本物の印が書いてあるので）。

差分画像では二重線が**途切れる**。線が枠線や文字と重なった部分が消えるためで、
差分から探すのは筋が悪い。そこで**元画像から長い横線だけを取り出し、
白紙にも同じ線があるもの（罫線）を引く**方式にした。

```
lines   = 元画像 を 横長カーネル(枠幅1.3倍×1) で opening
plines  = 白紙   を 同じカーネルで opening し、縦5px 横2px ぶん太らせる
written = lines AND NOT plines
```

白紙側を**縦に厚く**膨らませるのが要点。罫線は重ね合わせのずれで上下に数ピクセル動くので、
3x3では消えきらず「手書きの長い横線」に化ける。実際、これを直すまでは
本物の印の13.8%が二重線と誤判定され、どう調整しても差引マイナスだった。

さらに2つ条件を足した。

1. **枠の左右どちらにも突き抜けている**こと（`STRIKE_SIDE = 0.3`）。
   塗りつぶしの印は枠の中で横線の束になるため、これが無いと巻き添えになる。
2. **同じ項目に他の印があるときは緩く見る**（1.8 対 2.2 枠幅）。
   二重線で消す人はたいてい別の選択肢に付け直しているので、
   「2つチェックされている」状態は書き直しの手がかりになる。

結果 47件中39件を正しく未チェックにでき、巻き添えは9件に収まった。

### 7.6 残っている誤り（171件 / 18,600件）

| 中身 | 件数 | 見立て |
|---|---|---|
| 枠外はみ出し の見落とし | 31 | 枠の右下0.55〜0.95から外へ描かれる書き方。隣の手書きと見分けが付かない |
| 誤検出 | 117 | うち79件が3通（0022・0053・0085）に集中。地の汚れが多いスキャン |
| 二重線の取りこぼし・巻き添え | 17 | |

誤検出が3通に集中しているのは、**紙の地の汚れ**が差分に出るため。
枠ごとの規則では取り切れないので、直すなら「1通ぶんまとめて見て、
明らかに印が多すぎる紙はしきい値を上げる」といった別の層が要る。

---

## 8. つまずいた点（再発防止）

- `cv2.createThinPlateSplineShapeTransformer` は opencv-python-headless 5.0 に無い → 自前実装した
- OpenCV.js は `cv.ORB` のフル引数コンストラクタを公開していない
  （`Cannot construct ORB due to unbound types`）→ 既定で生成してセッターで調整する
- `zip(detected, BOX_ORDER)` は短い方に切り詰められるので、
  検出数が合っていないことに気付けなかった。個数の照合を先に入れるべきだった
- サイズの最頻値を全候補から取ると、小さな文字部品に引っ張られる（16px と出た。実際は21px）。
  「枠内が空の候補」に絞ってから取る。両ページ合算すると安定する

---

## 8.1 単一HTMLファイル版で OCR が動かない件

`file://` で直接開くと、ページの生成元が `null` になる。
このとき OCR ワーカー（Blob URL）から別の Blob URL を `importScripts` できず、
tesseract.js の初期化が失敗する。

試したが駄目だったもの:
- `workerBlobURL: false` を指定 → 同じエラー
- 言語データの取得も、ワーカー側からのfetchなので同様に失敗する

日本語のOCRモデル（onnxruntime-web）も同じ理由で入れていない。
ORT はワーカ用のコードを動的 `import()` で読むため `file://` では通らず、
入れても動かないのに 30MB 増えるだけだった。
（`ort.wasm.bundle.min.mjs` のように glue を同梱した版と `wasmBinary` を使えば
動く可能性はある。未検証）

ブラウザ側の制約なので回避できない。**縮退動作にした**。
`file://` を検出したらOCRを自動で無効にし、画面に説明を出す。
チェックボックス186項目（全項目の約8割）はそのまま読める。
OCRも必要なら Web版（サーバに置く）か Windows版インストーラを使う。

---

## 8.2 レビューで見つかった不具合（修正済み）

品質チェックの通し読みで、**診療情報が誤って出力される不具合**が見つかった。
どれも「見た目は正しく、確信度も高いのに中身が違う」型で、
確認画面の「要確認のみ表示」からも漏れるため質が悪い。記録として残す。

| # | 不具合 | 起きていたこと | 対処 |
|---|---|---|---|
| 1 | 定型文フィルタが臨床情報を消す | `認知症` `骨折` `がん` `転倒` `脱水` などが空になり、しかも確信度は高いまま。様式の定型文の中にその語が出てくるため「定型文の一部」と誤判定していた | 部分一致では捨てず、**定型文と長さが近い（0.70以上）行だけ**を対象にした |
| 2 | 病名が別の病名に化ける | `糖尿病` → `糖尿病性腎症`（特定疾病）を確信度100%で採用。読み取り結果が辞書語の一部でしかない場合も1.0になっていた | 辞書語の方が長い場合は**全体の編集距離**で評価。`骨粗症`→`骨粗鬆症`(0.75)は残し、`骨折`→`圧迫骨折`(0.50)は候補どまりにした |
| 3 | 読めない日付が「空欄・確信度0.9」 | 枠にインクがあるのに読めなかった場合も空欄扱いで高確信。要確認から漏れる | インクの有無を見て、読めなかった場合は確信度を下げて注記を付ける |
| 4 | 3桁以上の数字を切り詰め | `2024年` → `20年` → 西暦2038年。`245日` → `24日` | 桁数が合わないものは**捨てる**（読めなかった扱い） |
| 5 | ブラウザ版の丸囲み判定が2択で必ず失敗 | 中央値の取り方が `Math.floor(n/2)` で、n=2 では最大値になる。男・女の欄が常に「印なし」 | 偶数個は中央2つの平均にした |
| 6 | ブラウザ版で印刷済みの元号を上書き | `平` を誤読すると記入日が平成7年になり**30年ずれる** | Python 側と同じく、印刷済みの元号は固定にした |
| 7 | ブラウザ版に `date_digits` が無い | 未知の文字種が素通りし、Python が落とす元号がそのまま残っていた（6の原因） | 追加し、未知の文字種は警告を出すようにした |
| 8 | 丸囲み検出がPython側にしかない | 同じ画像で Python とブラウザの結果が違う | ブラウザ版にも実装した |
| 9 | 正規化のずれ | 半角カナ `ﾘｳﾏﾁ` が Python では 1.0、ブラウザでは 0.0 | ブラウザ側も NFKC 正規化にした |
| 14 | 対応点の比率判定でループ変数を間違い | `len(pairs[0])` を全組に適用。組によっては `ValueError` で落ちる | 組ごとに長さを確かめる |
| 18 | CSV の読取日時が出力時刻 | 全行が同じ時刻になる | 読み取り時刻を保持して出力 |

### 8.2.1 2回目のレビュー（読み取りの検算・操作ログ・トークンの追加後）

| # | 不具合 | 起きていたこと | 対処 |
|---|---|---|---|
| 1 | **記入が無い欄の拾い読みが素通り** | `摂食に関する留意事項＝い`（確信度 0.816＝緑「高」）、`褥瘡の部位＝一ーー一`。検算の「多すぎ」判定に `見込み上限 >= 1` の条件が付いていたため、**見込み0＝いちばん拾うべき場合だけ判定に入らなかった**。4検体で21欄、うち20欄が無警告 | 密度が1文字ぶんに満たない（0.25未満）のに文字が読めた場合を独立して判定し、確信度×0.35。文字種による除外もしない。実測で28欄を検出、確信度0.8以上で残るものは0件 |
| 2 | **操作ログにファイル名が残る** | `山田太郎_意見書.pdf` がそのまま CSV の「補足」列に出る。`pii` 指定も「値も記録する」の設定も効かない | ファイル名は記録しない（件数だけ）。例外の文面に混ざるファイル名も伏せる |
| 3 | **医療機関名が伏せ字にならない** | 一覧から選んだときだけ `shape()` を通しておらず、`pii: mask` を無視して生で残っていた | 他と同じく `shape()` を通す。自動で入る所在地・電話も記録するようにした |
| 4 | **編集ログが別の意見書と混ざる** | ブラウザ版のレコードに `id` が無く、待ち行列のキーが全件で同じになっていた。1件目を打ちかけて2件目に移ると、**別の患者の「直す前」と「直した後」を組み合わせた、存在しない訂正**が残る（4件中2件が消失） | レコードに目印を付け、件ごとにキーを分ける。件を切り替える前に打ちかけを確定させる |
| 5 | **欄が画像の端に掛かると読み取り全体が落ちる** | `widen_left` が空の切り抜きを `adaptiveThreshold` に渡して例外。欄ループに try が無いため、**チェック欄の結果も含めて1件まるごと失われる** | 範囲外・NaN を弾くガードを両版に入れ、欄ごとに try で囲って失敗した欄だけ要確認にする |
| 6 | 括弧を片方だけ落として病名を壊す | `（右）大腿骨頸部骨折（術後）` → `右）大腿骨頸部骨折（術後`。辞書照合の前に走るので `大腿骨頸部骨折` に当たらなくなる | 括弧は**対応が取れていないものだけ**落とす |
| 7 | 符号と「該当なし」が消える | `-3kg` → `3kg`（減ったのか増えたのか分からない）、単独の `-` → 空（未記入と区別できない） | 後ろが数字なら符号を残す。全部消える場合は元の文字列を返す |
| 8 | 注記が後段で上書きされる | LLM補正と暫定位置の2箇所が代入のままで、検算の理由が消え**減点だけが残る** | すべて `_add_note()` 経由に |
| 9 | ふりがなの訂正記録が消える | 直後の代入で `corrections` ごと置き換え。`trim_edges` の理由を混ぜたことで発生頻度が跳ね上がった（退行） | `extend` に |
| 10 | 訂正の理由が画面に出ない | `訂正: →（削除）` としか出ず、理由を持つ訂正が読めない | 理由だけの訂正はその文言を出す |
| 11 | トークンの説明が実態より強い | 「トークンを入れないと見えません」と書いていたが、`known()` が他人のトークンを返しており、切り替えれば開けた | 件数だけを返すようにし、説明を直した。**他の記録を実際に消す導線**を管理画面に追加 |
| 12 | 移行が黙って前の利用者のデータを渡す | 更新後に最初に開いた人が、前任者の読み取り結果と操作ログを引き継ぐ | 引き継いだ場合は画面に知らせる |
| 13 | 空白の扱いが両版でずれる | Python の `strip()` は U+0085 を落とし U+FEFF を落とさない。`trim()` は逆。4035文字列で252件不一致 | 同じ文字集合を明示して揃えた（31ケースで一致を確認） |
| 14 | 差分画像を欄ごとに作り直していた | 1ページ3.9MB の生成が29回。Python でページあたり0.3秒 | ページに1回だけ作って使い回す |
| 15 | 書き出すと理由が消える | `note` `corrections` `expected_chars` が JSON に入っておらず、読み直すと減点だけが残る | 出力・取り込みの両方に加えた |

**このレビューは実測で行った。** ブラウザ（Chromium＋同梱の opencv.js）で `engine.js` を
動かし、Python と同じ warped/blank 画像に対して41欄を突き合わせて一致を確認、
異常な矩形12種×200周で Mat の解放漏れが無いことも確認している。

丸数字 `④` で落ちる不具合も後から見つかった。`str.isdigit()` は `④` に True を返すが
`int()` は失敗する。**半角数字だけを数字として扱う**ようにした。

教訓: 同じ判定を2つの言語に持つ以上、**両方で同じ入力を通して値が一致するか**を
確かめないと、片方だけ壊れていても気付けない。

---

## 8.3 3回目のレビューで見つかった不具合（修正済み）

正解データでの精度向上・チェック欄の言葉・VLM・様式の形のJSON を足したあとの見直し。

| # | 症状 | 原因 | 直し方 |
|---|---|---|---|
| 1 | 「→」「℃」「∇」が長音「ー」に変わる | 文字クラスの `‐-─` が **U+2010〜U+2500 の範囲**として解釈され、約1,300文字が該当していた。ブラウザ版はエスケープ済みで、**Python 版だけが違う結果**を出していた | ハイフンをエスケープ。20ケースで両版の一致を確認（ISSUE #56） |
| 2 | VLM の出力抑止が入れ違う恐れ | `_silence()` はプロセス全体のファイル記述子を差し替えるため、同時に走ると復帰が入れ違い、標準出力が `/dev/null` に固定される | 錠をかけた。サーバ側は `_vlm_lock` で直列化しているが、ライブラリとして単体で壊れないようにする |
| 3 | ラベル絞り込みの無駄な変数 | 集合を作ってから空かどうかだけを見ていた（ブラウザ版は `some()` で済ませている） | `any()` に。両版の書き方を揃えた |
| 4 | 単一HTML版だけ `labels.js` が入らない | 読み込むスクリプトの並びを `build_standalone.py` に**手書き**していた。通常版は `js/` をそのままコピーするので動き、**単一HTML版を開くまで気付けない** | 並びを `index.html` から取り、足りなければビルドを止める（ISSUE #57） |
| 5 | VLMで日付欄を読み直すと西暦が古いまま | 値の文字列だけを差し替えていたので `date` / `gregorian` が残り、CSVの西暦列と画面が食い違う | 読み直したあとに和暦を解析し直す |

1 が一番危ない種類の不具合。**文字クラスの中のハイフンは端に置くかエスケープする。**
非ASCIIの文字を並べるときは範囲になってもエラーにならないので気付けない。

4 の教訓は **同じ一覧を2か所に書かない**。片方を直し忘れたときに、
気付きにくい方だけが壊れる。

---

## 9. これから

- [ ] `sample_v1` のヘッダ部テキスト欄座標を管理画面で確定させる（3.3）
- [x] 正解データを作って精度を数値化（7.3）
- [ ] Python版とブラウザ版の判定結果が一致するかを自動で確かめる仕組み（8.2の教訓）
      ※チェック欄は node + opencv.js で558枠を突き合わせ、ずれ0を確認済み
- [x] tesseract（jpn）を入れた環境での精度比較（4.1.1）
- [ ] 管理画面のテンプレート編集で、チェックボックスも個別に動かせるようにする
- [ ] 「枠外はみ出し」の印を拾う（正解データで45.6%。ISSUE #55）
- [ ] 地の汚れが多い紙を1通単位で見分けてしきい値を上げる（7.6 / ISSUE #55）
- [ ] チェック欄の言葉の食い違いを、2文字以下の言葉でも見つける（2.4.2 の制限）
- [ ] 複数件の一括読み取り時の進捗表示の改善（現状は件単位）
